# IDE (集成开发环境) 相关文件
.idea/                     # IntelliJ IDEA 项目文件
*.iml                      # IntelliJ IDEA 模块文件
.vscode/                   # Visual Studio Code 配置文件目录
*.suo                      # Visual Studio 解决方案用户选项文件
*.ntvs*                    # Node.js Tools for Visual Studio 文件
*.njsproj                  # Node.js Tools for Visual Studio 项目文件
*.sln                      # Visual Studio 解决方案文件
*.sw?                      # Vim 编辑器产生的交换文件

# 操作系统自动生成的文件
.DS_Store                  # macOS Finder 文件夹自定义属性文件
Thumbs.db                  # Windows 缩略图缓存文件
ehthumbs.db                # Windows 媒体文件缩略图缓存
Desktop.ini                # Windows 文件夹自定义配置文件

# 日志文件
*.log                      # 所有以 .log 结尾的文件
logs/                      # logs 目录下的所有文件
npm-debug.log*             # npm 调试日志文件
yarn-debug.log*            # yarn 调试日志文件
yarn-error.log*            # yarn 错误日志文件

# 依赖包目录 (通常很大，不需要版本控制)
node_modules/              # Node.js 项目依赖包目录
jspm_packages/             # jspm 包管理器目录
bower_components/          # bower 包管理器目录

# 构建/打包/编译产物目录
target/                    # Maven/Java 项目构建输出目录
dist/                      # Vue/React/Angular 等前端项目打包输出目录
build/                     # 通用构建输出目录
out/                       # 一些 IDE (如 IntelliJ) 的编译输出目录

# Maven 相关文件
pom.xml.tag                # Maven release 插件产生的标签文件
pom.xml.releaseBackup      # Maven release 插件产生的备份文件
pom.xml.versionsBackup     # Maven versions 插件产生的备份文件
pom.xml.next               # Maven release 插件产生的下一个版本文件
release.properties         # Maven release 插件产生的属性文件
dependency-reduced-pom.xml # Maven shade 插件产生的简化 POM 文件
buildNumber.properties     # Maven build number 插件产生的属性文件
.mvn/                      # Maven Wrapper 配置目录

# Java 相关文件
*.class                    # Java 编译后的字节码文件
*.jar                      # Java 打包后的 Jar 文件
*.war                      # Java Web 应用打包后的 War 文件
*.ear                      # Java 企业应用打包后的 Ear 文件
hs_err_pid*                # Java 虚拟机崩溃时产生的错误日志文件

# Node/npm 缓存和临时文件
.npm/                      # npm 缓存目录
.node_repl_history         # Node.js REPL 历史记录文件

# 环境变量配置文件 (非常重要，通常包含敏感信息，不应提交)
.env                       # 主环境变量文件
.env.*                     # 所有以 .env. 开头的文件 (如 .env.local, .env.production)
!.env.example              # 不忽略 .env.example 文件 (感叹号 ! 表示例外，通常用于提交配置模板)
!.env.development          # 不忽略 .env.development 文件 (按需取消注释)
!.env.production           # 不忽略 .env.production 文件 (按需取消注释，但不推荐提交生产环境配置)
# 如果你想忽略所有 .env 文件但保留 .env.example，可以使用更简洁的：
# .env*
# !.env.example

# 用户上传文件目录 (如果上传文件存储在本地且不想提交)
# uploads/                 # 例如，存放用户上传文件的 uploads 目录

# 针对项目结构的特定忽略 (可选，通用规则通常已覆盖)
# job/target/              # 明确忽略后端构建目录
# job-web/dist/            # 明确忽略前端构建目录
# job-web/node_modules/    # 明确忽略前端依赖目录
# job/logs/                # 明确忽略后端日志目录
# job/uploads/             # 明确忽略后端上传目录

# IntelliJ IDEA 特定文件 (如果 .idea/ 未完全覆盖)
*.iws                      # IntelliJ IDEA 旧版工作区文件
*.ipr                      # IntelliJ IDEA 旧版项目文件

# Eclipse 特定文件
.classpath                 # Eclipse 项目类路径文件
.project                   # Eclipse 项目配置文件
.settings/                 # Eclipse 项目设置目录

# NetBeans 特定文件
nbproject/                 # NetBeans 项目文件目录
build/                     # NetBeans 构建输出目录 (可能与通用规则重复)
nbbuild/                   # NetBeans 构建相关文件
dist/                      # NetBeans 打包输出目录 (可能与通用规则重复)
nbdist/                    # NetBeans 打包相关文件
.nb-gradle/                # NetBeans Gradle 相关文件 